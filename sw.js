var GHPATH = '/colour-palette-test-app';

var APP_PREFIX = 'cpta_';
 
var VERSION = 'version_00';
 
var URLS = [    
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/style.css`,
  `${GHPATH}/script.js`
]